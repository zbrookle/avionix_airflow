name: avionix_airflow
dependencies:
  - pip

  # Test requirements
  - black
  - flake8
  - mypy
  - isort
  - pytest
  - pandas
  - requests
  - pytest
  - pytest-rerunfailures
  - pytest-cases
  - pip:
    # Main requirements
    - avionix==0.4.4
    - python-hosts==1.0.0
    - cryptography==3.0
